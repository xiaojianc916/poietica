# 2026-08-28

## 架构审查与模块化总体设计（RFC 0001）

- 用户要求：全仓架构审查 + 模块化总体设计 + 两棵目录树（英文正本 / 纯中文语义对照），参考 Lody / opencode / egoist/waku。
- 计划模式完成审查与设计；用户确认四项决策：RFC+HTML 双交付、全仓范围、本轮只做设计不动代码、中文树=语义对照视图。
- 关键发现：分层纪律健康（跨包深引用 0 违规、kap-projection 唯一分发点成立）；四大动作=拆 driver.rs（2580 行，判据①③，crate 内拆 event_router.rs+rest.rs）、file-diff 并入 core（9 处消费者）、transcript-store 写点收敛（单一 #commit，不拆文件）、ipc/agent 导出面收窄；治理补漏=update crate 漏在 nativeCrates 名单外、ADR 两个 0030 重号。
- 核实闭环：driver.rs L1768-2449 确为 18 个纯 REST 端点函数（reqwest）；FORMATS 正本在 commands/asset.rs:354，宪法引用无误；mascot 包级消费者仅 agent-ui（settings 的 mascot-prefs 只依赖 core），不拆。
- 外部调研结论：egoist/waku 是 Rust+GPUI 本地 agent 桌面应用（非 React 框架）；durable/live 双流实质已存在（run_events + journal.rs:23 攒批）只缺命名；协议真源/回放键/依赖红线均已有等价物，勿重复建设。
- 交付物：docs/rfcs/0001-modular-convergence.md（正本）、CONTEXT.md（仓库根术语表，新建）、%TEMP%\architecture-review-20260828-202833.html（评审用）。
- 分批路线图（7 批，每批 bun run check 独立验收）：①治理补漏 ②file-diff 并入 ③driver 拆分 ④transcript-store 写点 ⑤导出面收窄 ⑥CONTEXT+双流命名 ADR ⑦agent_setup 收敛进 agent-runtime。
- 注意：本轮未动任何代码；review/browser 热区冻结观察。

## 重构批次 1-2 · crates/conversation 编译修复

- 症状：`cargo test`（problem/time/conversation/ledger 四 crate）报 `variant field name 'kind' conflicts with internal tag` + 连锁 Serialize/Deserialize bound 失败。
- 根因：serde 内部 tag 枚举（`#[serde(tag = "kind")]`）的变体字段不能与 tag 同名；`UnsupportedExternalEvent { kind: String }` 撞名。该 crate 是重构脚本 refactor.mjs 生成的未跟踪新 crate，从未编译过，无落盘数据，改字段名无兼容负担。
- 修法：字段改 `raw_kind`（wire 名 camelCase → `rawKind`），一处定义 + projection_replay.rs 测试构造；同步修掉 refactor.mjs 里的两份副本（幂等脚本按"一次换干净"不留错误源）。
- 验证：四 crate cargo test 全绿（conversation：projection_replay 2 通过、turn_state_machine 4 通过）。
- 沉淀：serde 内部 tag 撞名是编译期错误，本项目后续新增 tag 枚举变体字段时需避开 tag 名；refactor.mjs 是批次 1-2 的幂等生成脚本，改生成物需同步改脚本。

## TS 侧修复：@poietica/tools typecheck + biome（check:web 达成 30/30）

- 9 个 tsc 错误：7×TS4111（索引签名方括号访问：candidate['host'] 等 4 处 + process.env['KIMI_CODE_HOME'/'KAP_ORIGIN'/'CI'] 3 处，与 agent-catalog 既有惯例一致）；1×TS18048 first possibly undefined（两处报点）。
- TS18048 根因：工作区生效 TS 7.0.2 原生编译器，never 返回函数（die）调用点窄化不可靠；修法 `return die(...)` 早返回，只依赖普通 return CFA。
- biome：clean.ts 的 noImplicitAnyLet 用 `await readdir(...).catch(() => undefined)` 重构消除；biome.json overrides 把 tools CLI 脚本的 noConsole 豁免从 `tools/**/*.mjs` 扩到含 `.ts`（本批 tools .mjs→.ts 迁移，同一意图）；kap-spec-sync.ts import 排序+超长行 biome 自动修。
- 验收：`bun run check:web` 30/30 全绿（biome ci 全仓 + 架构规则 + 全工作区 typecheck+test）。


