# 0034 辅助面板是工作台 Part

- 状态：已接受
- 日期：2026-08-31

右侧列由工作台拥有，浏览器、审查、终端、辅助对话和上下文子代理通道只是它的 pane。
主动入口只来自 catalog；delegate 不进 catalog 的 launcher，只响应主时间线工具调用。
零标签是启动器，不再由浏览器隐式制造空白标签。浏览器网页与 child webview 的唯一真相仍在原生 host。
