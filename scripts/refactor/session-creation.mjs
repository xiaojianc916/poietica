export function migrateSessionCreation(m) {
  const driver = 'crates/agent-runtime/src/driver.rs'
  m.replace(driver, `        // 5. 建锚会话（REST）。sessionCreateSchema：metadata.cwd 与\n        //    workspace_id 至少给一个。\n        let session = match post(\n            &http,\n            &format!(\"{base_url}/sessions\"),\n            &json!({ \"metadata\": { \"cwd\": cwd.to_string_lossy() } }),\n        )\n        .await`, `        let bootstrap = bootstrap_profile(&http, &base_url).await?;\n        let session = match post(\n            &http,\n            &format!(\"{base_url}/sessions\"),\n            &json!({ \"metadata\": { \"cwd\": cwd.to_string_lossy() }, \"agent_config\": bootstrap }),\n        )\n        .await`)
  m.replace(driver, `        // 5.5 绑模型。放在订阅之前：这是开会话的收尾，不是回合的一部分，\n        //     它引发的那几帧不该记进第一轮。\n        ensure_model(&http, &base_url, &session_id).await;\n\n`, ``)
  m.replace(driver, `                        Some(Command::NewSession { cwd: new_cwd, reply }) => {\n                            let http2 = http.clone();\n                            let base2 = base_url.clone();`, `                        Some(Command::NewSession { cwd: new_cwd, reply }) => {\n                            let http2 = http.clone();\n                            let base2 = base_url.clone();\n                            let anchor = session_id.clone();`)
  m.replace(driver, `                                let result =\n                                    open_session(&http2, &base2, &new_cwd, &book2, &ws2).await;`, `                                let result = async {\n                                    let profile = session_profile(&http2, &base2, &anchor).await?;\n                                    open_session(&http2, &base2, &new_cwd, &profile, &book2, &ws2).await\n                                }.await;`)
  m.replace(driver, `async fn open_session(\n    http: &reqwest::Client,\n    base_url: &str,\n    cwd: &Path,\n    book: &SessionBook,`, `async fn open_session(\n    http: &reqwest::Client,\n    base_url: &str,\n    cwd: &Path,\n    profile: &Value,\n    book: &SessionBook,`)
  m.replace(driver, `        &json!({ \"metadata\": { \"cwd\": cwd.to_string_lossy() } }),`, `        &json!({ \"metadata\": { \"cwd\": cwd.to_string_lossy() }, \"agent_config\": profile }),`)
  m.replaceAll(driver, `\n    ensure_model(http, base_url, &id).await;\n`, `\n`, 2)
  m.replace(driver, `\n    ensure_model(http, base_url, session_id).await;\n`, `\n`)
  m.section(driver, `/// 给这条会话绑上模型。`, `/// agent 报它卡在审批上时，把这条会话挂着的审批逐个请上桌。`, `async fn bootstrap_profile(http: &reqwest::Client, base_url: &str) -> Result<Value> {\n    let config = get(http, &format!(\"{base_url}/config\")).await?;\n    let model = config.get(\"default_model\").and_then(Value::as_str).map(str::trim).filter(|value| !value.is_empty()).ok_or_else(|| KapError::Transport { message: \"this Kimi installation has no default model\".to_owned() })?;\n    Ok(json!({ \"model\": model }))\n}\n\nasync fn session_profile(http: &reqwest::Client, base_url: &str, session_id: &str) -> Result<Value> {\n    let status = get(http, &format!(\"{base_url}/sessions/{session_id}/status\")).await?;\n    let mut profile = serde_json::Map::new();\n    for (from, to) in [(\"model\", \"model\"), (\"thinking_level\", \"thinking\"), (\"permission\", \"permission_mode\")] {\n        if let Some(value) = status.get(from).and_then(Value::as_str) && !value.is_empty() { profile.insert(to.to_owned(), Value::String(value.to_owned())); }\n    }\n    for field in [\"plan_mode\", \"swarm_mode\", \"tower_mode\"] {\n        if let Some(value) = status.get(field).and_then(Value::as_bool) { profile.insert(field.to_owned(), Value::Bool(value)); }\n    }\n    Ok(Value::Object(profile))\n}\n\n/// agent 报它卡在审批上时，把这条会话挂着的审批逐个请上桌。`, `async fn bootstrap_profile`)
}
