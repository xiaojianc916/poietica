# Poietica 工程指南

读者是修改这个仓库的人与 AI。它回答三件事：产品的不变量是什么，架构边界在哪，
改完怎么证明没有弄坏东西。

## 这份文件的权限

本文**解释**架构，不**定义**架构。可执行的定义只在这些地方：

| 事实 | 定义在 | 由谁执行 |
| --- | --- | --- |
| 包分层与依赖方向 | `tools/architecture/rules.config.mjs` | `pnpm test:architecture` |
| 包内目录命名 | `tools/architecture/rules.config.mjs` 的 `forbiddenDirectoryNames` | `pnpm test:architecture` |
| 依赖版本 | `pnpm-workspace.yaml` 的 catalog | pnpm |
| IPC 契约 | Rust 侧类型，导出到 `packages/ipc/src/generated/` | `pnpm ipc:check` |
| 磁盘布局 | `apps/desktop/src-tauri/src/paths.rs` | 运行时 |

与本文冲突时以上表为准，并回来修本文。

**不要在这里重抄任何一张表。** README、本文、`docs/architecture/README.md` 与
`tools/architecture/README.md` 曾各存一份手抄分层表，四份互相矛盾 —— 其中一份列了
磁盘上不存在的包，另一份漏掉了一个真实存在的包，而被执行的始终只有配置那一份。
手抄表不会让事实更清楚，只会制造第二个事实。

## 产品不变量

Poietica 是本地优先的 AI agent 桌面环境。会话是产品主界面；工具、Skill 与 MCP 服务于会话。

- **会话是唯一中心。** 任何能力不得绕过会话另立入口。
- **用户主导。** AI 扩展可能性，不替用户决定。对用户作品的改动必须先成为可预览、可拒绝
  的提案；一旦应用，走该产品域的正常写入路径，因而可撤销。
- **事件日志是运行的事实来源。** 会话更新先持久化再渲染，中断的运行可以重放。线程、运行、
  工具调用与权限记录都是它的投影，不得另存第二份。
- **本地优先。** 状态可靠落盘，行为可预测。
- **边界清晰。** 每一类状态有且只有一个所有者、一条写入路径。

AI 不得：静默修改用户的文本、文件或会话内容；把生成结果当作比用户判断更权威的结论；
在未说明的情况下携带用户内容发起网络请求；暴露密钥、Provider 内部信息、敏感 Prompt 或
原始堆栈；创建第二套会话或运行状态来源。

## 架构

### 形状

```text
apps/desktop/src/        产品界面与应用编排
apps/desktop/src-tauri/  唯一的组合根：建窗、注册命令、在边界上做 DTO 与领域类型互转
crates/                  native crate，与宿主无关，可脱离 Tauri 单独测试
packages/                TypeScript 工作区包，分层，依赖单向向下
tools/architecture/      机器执行的那部分架构
```

### 为什么按层切，而不是按技术种类切

分层的依据是**依赖能指向哪里**，不是文件长什么样。按技术种类切会切出
`components` / `services` / `utils` 这种谁都能往里塞的桶：桶不声明边界，于是没有边界。
按依赖方向切，一个包属于哪层由「它被谁依赖、又依赖谁」决定，这个问题有唯一答案。

层名与包名在配置里，这里只写三条不变量：

- **依赖只能指向更低层。** 同层互指默认禁止，仅有的例外逐条写在配置的
  `sameLayerDependencies` 里并附理由；对应的 manifest 边消失了，豁免就得删。
  判据落在各包 `package.json` 的依赖块上，不落在源文件的 import 上。
- **只有 transport、composition 与 application 三层可以直连 `@tauri-apps/*`。**
  平台能力一旦渗进领域包或基础包，那个包就再也无法在非 Tauri 环境里测试或复用。
- **跨包只走公开 exports。** 不 deep import 到别人的 `src/`，不用相对路径跨包，也不用
  包名引用自己 —— 最后这条看着无害，实际会让包入口与内部模块互指成环。

新增一个包，先在分层表里给它定层，否则架构检查直接失败。不定层的包不受任何方向约束，
那比没有这个包更糟。

### 包内怎么分目录

目录名必须声明**能力**，不是**技术种类**。禁用清单在
`tools/architecture/rules.config.mjs` 的 `forbiddenDirectoryNames`，由
`pnpm test:architecture` 执行。**本文不重抄它** —— 上面已经说过为什么。

两类名字被禁：DDD 的层名，因为包边界已经承担了分层，包内再套一层就是两套架构叠着；
以及万能桶名和按技术种类起的名，它们不声明任何边界，最终什么都往里塞。

**这是一份黑名单，不是白名单。** 判据只有两条：不在禁用清单里，且说得出自己是什么
能力。`composer`、`timeline`、`minimap`、`persistence` 都是好名字。

### 原生侧

每个 native crate 拥有一块与宿主无关的能力，互不依赖，都不依赖 `tauri`，因而都能用
普通 `cargo test` 单独测。`src-tauri` 是唯一的组合根：命令函数是薄封装，业务分支下沉到
crate。分层与每个 crate 的职责见 `docs/architecture/rust-layers.md`。

### IPC

DTO 必须显式、类型安全、可验证。禁止 `serde_json::Value`、任意路径、第三方对象、原始
错误或未经验证的模型输出直接穿透 IPC。每个原生命令必须定义能力范围、输入校验、错误语义、
取消机制与幂等性。

TypeScript 侧的绑定是生成物，不手改：改完 Rust 侧类型跑 `pnpm ipc:generate`，
`pnpm ipc:check` 会证明两侧没有漂移。

## AI、网络与安全边界

- Provider 调用、密钥与网络传输隔离在显式接口之后。
- AI 上下文必须是有意选择、尽可能最小、可检查的。
- 模型响应是不可信输入，结构化输出先验证再使用。
- 模型输出不得直接触发原生命令、任意网络请求、文件写入或不受限制的工具调用。
- 长时间运行的 AI 工作必须支持取消、超时与过期结果保护。
- WebView、Worker、插件与 AI 集成只获得完成任务所需的最小能力。

文件、路径、SVG、图片、字体、剪贴板内容、导入数据、AI 响应与插件内容一律视为不可信输入，
在边界处校验 schema、权限、大小、路径与资源预算。

## 你会撞上的墙

`pnpm test:architecture` 是机器执行的那部分架构。它不给建议，只报违规位置。规则清单与
判据见 `tools/architecture/README.md` —— 这里说的是撞上去意味着什么。

- **方向墙。** 依赖指向了更高层，或非白名单包碰了 `@tauri-apps/*`。这是分层被破坏，不是
  允许集漏了谁：先想清楚这个能力该归谁，不要去改允许集。
- **入口墙。** deep import、跨包相对路径、包名引用自己。包入口是给外部看的那道边界，
  自己人绕过去，这道边界就是假的。
- **目录名墙。** 见上一节。
- **manifest 契约。** 工作区 manifest 只有一套写法：`exports` 与 `main`/`types` 不并存；
  子路径名由目标路径推导；`sideEffects` 的 glob 带目录前缀；同一段脚本不给两个名字；
  一个脚本里不用 `&&` 串两次同一个程序（编排是 turbo 的活）；版本只能是 `catalog:`
  或精确号。
- **全局声明唯一。** 通配符模块声明写在哪个包里，效果都是整个编译单元，所以全仓只能有
  一份。需要为某种资源补声明时，放到拥有这类资源的那个包里，用三斜线引用投送出去，
  不要新开顶层目录。
- **文档命令真实存在。** 文档里写的带冒号 pnpm 脚本必须真的在某份 manifest 里。

规则是数据不是程序：加一条 = 往 `rules.config.mjs` 加一个对象，**不是**加一个脚本。
独立的 `check-*.mjs` 本身就是违规 —— 它把一次迁移编码成文本快照，迁移结束后继续留着，
腐烂时不会失败。

判据必须只依赖单个文件能证明的形状。「某某没有调用方」这类需要穷举全仓调用路径的命题
不要写成规则：它做不到，最后会变成硬编码的事实断言，而事实会变。

## 改之前

1. 想清楚用户可见行为是什么、这块状态归谁、落在哪个包。
2. 搜一遍现有模型与写入路径，别造第二条。
3. 判断是否触及 AI 上下文、持久化、IPC、权限或公开 API —— 触及就要记 ADR 或 RFC。
4. 范围保持聚焦，除非正确性要求扩大。

## 改的时候

1. 单一事实来源，单一写入路径。
2. 先定用户行为与模块契约，再写内部实现。
3. 包、IPC、Provider 与文件边界上用显式接口。
4. **替换旧实现时，在同一次改动里删掉旧路径。** 不留无期限双轨，不用兼容层掩盖职责不清。
5. AI 提案可审阅、可取消、可撤销。

禁止：空 `catch`；隐藏副作用；万能 Store 或万能 Manager；绕过公开 API 或架构测试；
未经请求扩大改动范围；未验证就宣称完成。

## 改完之后

```bash
pnpm check
```

一条命令。它串起 `check:web`（Biome、架构规则、全工作区 typecheck 与 test）与
`check:rust`（rustfmt、Clippy、cargo test、IPC 绑定一致性）。展开定义在根 `package.json`，
这里不重抄。

涉及 AI 的改动还必须验证：取消、超时、异常模型输出、过期结果、用户确认，以及敏感数据与
密钥不泄漏。

## 完成的判据

- `pnpm check` 全绿；
- 每一类状态仍然只有一个所有者、一条写入路径；
- 没有新增双轨实现或兼容层；
- AI 行为有边界、可审阅、可撤销；
- 失败、取消与恢复路径都想过了；
- 文档、契约、实现与测试一致 —— 包括本文。

## 文档地图

| 位置 | 内容 |
| --- | --- |
| `docs/architecture/` | 稳定的系统边界与实现约束 |
| `docs/adr/` | 已接受的技术决策及其理由 |
| `docs/rfcs/` | 进行中的技术与产品提案 |
| `docs/runbooks/` | 开发、维护与运维流程 |
| `tools/architecture/README.md` | 机器执行的那部分架构 |
