# Review

Owns Git review read models, edit intent, diff projection, and observation lifetimes. Git is authoritative. Commands trigger a refresh through the same read pipeline; a command response never overwrites the current read model. The root export is framework independent; /surface owns React rendering and its worker. The native bridge implements the injected gateway. Core code cannot depend on the surface or application composition.
