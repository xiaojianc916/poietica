# @poietica/design-system

**我是什么**：表现基座。设计令牌、两档主题与无业务控件，全仓视觉与交互词汇的唯一产地。

**我拥有什么**：tokens/ 的令牌与主题值、styles.css（表层与滚动条的装配入口）、
control/ 的控件与浮层、layout/ 的区域分隔条、mark/ 的文件/集成/本地字形、
theme/ 的主题施加。

**谁允许调用我**：表现环（*-ui）与组合根（apps/desktop/src）。

**我不许知道什么**：任何领域概念、账本、协议、Tauri。不持业务状态，不替产品
选主题值——值归令牌，施加归调用方。
